<h1>Listing Blogs</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th>Image</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for blog <- @blogs do %>
    <tr>
      <td><%= blog.title %></td>
      <td><%= blog.body %></td>
      <td><%= blog.image %></td>

      <td>
        <%= link "Show", to: Routes.admins_blog_path(@conn, :show, blog) %>
        <%= link "Edit", to: Routes.admins_blog_path(@conn, :edit, blog) %>
        <%= link "Delete", to: Routes.admins_blog_path(@conn, :delete, blog), method: :delete, data: [confirm: "Are you sure?"] %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Blog", to: Routes.admins_blog_path(@conn, :new) %></span>
